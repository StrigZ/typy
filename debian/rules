#!/usr/bin/make -f

%:
	dh ${@}

override_dh_auto_build:
	for po_file in $(wildcard po/*.po); do \
	    lang=$$(basename $$po_file .po); \
	    mkdir -p usr/share/locale/$$lang/LC_MESSAGES; \
	    msgfmt -o usr/share/locale/$$lang/LC_MESSAGES/uncom-app-template.mo $$po_file; \
	done
	dh_auto_build